class Solution {
    public int mostWordsFound(String[] sentences) {
        int max = 0;
        for(String eachSentence : sentences){
            max = Math.max(max, eachSentence.split(" ").length);
        }
        return max;
    }
}
